name: CV Efremov Evgeney
on: [workflow_dispatch]

jobs:
    run-first-command:
        runs-on: ubuntu-latest
        steps:
            - name : Display some text
              run: echo "Some text" 
            - name : Command in multiline
              run: |
                node -v
                npm -v
