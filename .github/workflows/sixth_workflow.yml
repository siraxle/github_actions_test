name: Sixth Workflow
on: 
    push:
        branches:
            - main

jobs:
    run_first_command:
        runs-on: ubuntu-latest
        steps:
            - name: Event Vars
              run: |
                echo $GITHUB_SHA
                echo $GITHUB_REF
