name: Third Workflow
on: [push]

jobs:
    run-first-command:
        runs-on: ubuntu-latest
        steps:
            - name : List files and copy repo
              run: ls -a
            - uses: actions/checkout@v6
            - name : List files
              run: ls -a